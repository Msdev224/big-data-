# Checklist de rendu

- Vérifier que la base `BOOKSHOP` existe dans Snowflake
- Vérifier que les schémas `RAW`, `STAGGING`, `WAREHOUSE`, `MARTS` existent
- Vérifier que les 5 tables `stg_*` sont créées
- Vérifier que les 3 tables `dim_*` sont créées
- Vérifier que les 5 tables `fact_*` sont créées
- Vérifier que `MARTS.obt_sales` contient des données
- Préparer des captures d'écran Airflow
- Préparer des captures d'écran Snowflake
- Préparer des captures d'écran du dashboard
- Relire le rapport PDF
- Reprendre le plan PowerPoint
- Répéter le script vidéo
